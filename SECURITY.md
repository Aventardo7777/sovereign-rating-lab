# Security Policy

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 0.1.x | ✅ 接收安全更新 |
| < 0.1 | ❌ 不再支持 |

## 报告漏洞

**请勿通过公开的 GitHub Issue 报告安全漏洞。**

请使用以下任一私密渠道：

1. **GitHub 私密漏洞报告**（推荐）
   <https://github.com/Aventardo7777/sovereign-rating-lab/security/advisories/new>

2. **邮件**
   发送至 `176105827+Aventardo7777@users.noreply.github.com`，
   主题前缀 `[SECURITY] sovereign-rating-lab`。

### 请包含以下信息

* 漏洞类型（例如：依赖投毒、路径穿越、任意代码执行、凭证泄露）
* 受影响的文件、函数或依赖及版本
* 复现步骤（最小可复现示例最佳）
* 影响范围与可能的攻击场景
* 如果你有修复建议，请一并说明
* 是否希望被列入致谢名单

### 我们的承诺

| 阶段 | 目标时限 |
| --- | --- |
| 确认收到报告 | 3 个工作日内 |
| 初步评估与严重性定级 | 7 个工作日内 |
| 修复或缓解方案 | 30 个工作日内（严重问题优先） |
| 公开披露 | 修复发布后协调进行 |

我们会在修复发布后在 `CHANGELOG.md` 的 `Security` 段落中致谢报告者（除非你要求匿名）。

## 本项目特有的安全考量

### 1. API 密钥管理

本项目**绝不**在源码或配置文件中硬编码密钥。FRED 等需要凭证的数据源通过环境变量
读取（见 `src/config.py` 的 `get_env`）。

如果你是贡献者，请确认：

* 提交前运行 `git diff --staged` 检查是否误带密钥；
* 不要把 `.env` 文件加入版本控制（`.gitignore` 已覆盖）；
* 一旦误提交，**立刻轮换该密钥**——从 Git 历史中删除并不足以撤销泄露。

### 2. 网页抓取

`src/utils/http.py` 与 `src/ingest/ratings.py` 会发起网络请求。使用前请注意：

* 抓取代码**强制校验 `robots.txt`**，被禁止的路径会抛出 `RobotsDisallowedError`；
* 默认限速 2 秒/请求，请勿为了速度移除该限制；
* 原始响应会缓存到本地 `data/raw/cache/`，可能包含来自目标站点的内容；
  **请勿把抓取到的第三方内容提交到本仓库**，这可能构成版权侵权。

### 3. 数据文件反序列化

* 本项目只读取 CSV / JSON / YAML，**不使用 `pickle` 或 `joblib` 反序列化外部文件**。
* 如果你需要加载他人提供的 pickle 模型，请意识到 pickle 可执行任意代码——
  这是该项目的有意设计取舍，请自行评估来源可信度。

### 4. 依赖供应链

* 依赖版本使用下限约束（`>=`）。如需完全可复现的环境，
  请自行 `pip freeze > requirements.lock` 并在你的环境中固定。
* CI 会对每个 PR 运行 `ruff`、`black` 与 `pytest`，但**不**自动执行外部贡献者的代码
  以外的任何脚本。
* 如果你发现某个依赖存在已知漏洞（CVE），请开 Issue 或 PR 升级版本，
  并在 PR 描述中附上 CVE 编号。

### 5. 模型输出与免责

本项目输出的是**研究性统计结果**，其安全影响主要体现在被误用：

* 模型输出**不是投资建议**，不构成任何形式的金融意见；
* 请勿把本项目的输出直接用于信贷决策、交易执行或监管报送；
* 评级数据的所有权利归各评级机构所有，使用者需自行确认使用权限。

## 致谢

感谢所有负责任地披露安全问题的研究者。
